import {StyleSheet} from 'react-native';
import {Colors, Metrics} from '../../theme';

const {horizontalScale} = Metrics;

export default StyleSheet.create({
  keyboardAvoiding: {
    flex: 1,
    backgroundColor: Colors.DarkBlue,
  },
  largeLogo: {},
});
